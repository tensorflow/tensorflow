// RUN: fusion_to_xtile %s --xla_cpu_use_new_xtile_lowering | fusion_compiler_opt \
// RUN:   --xtile-cpu-new-xtile-to-vector --xla-simplify-arith \
// RUN:   | FileCheck %s --check-prefix=NEW-VECTOR
// RUN: test_correctness %s --xla_cpu_use_new_xtile_lowering

fusion {
  p0 = s32[] parameter(0)
  ROOT broadcast = s32[128,64] broadcast(p0), dimensions={}
}

ENTRY main {
  p0 = s32[] parameter(0)
  ROOT fusion = s32[128,64] fusion(p0), kind=kLoop, calls=fusion,
    backend_config={
      "fusion_backend_config":{
        "block_level_fusion_config":{
          "output_tiles":[{"sizes":["4", "4"]}],
          "num_ctas":"1"
        }
      }
    }
}

// CHECK:        xtile.entry_func @wrapped_fusion(
// CHECK-SAME:     %[[ARG0:[^:]+]]: memref<i32>,
// CHECK-SAME:     %[[ARG1:[^:]+]]: memref<128x64xi32>,
// CHECK-SAME:     %{{.*}}: index)

// CHECK:        scf.for %[[IDX:.*]] = %{{.*}} to %{{.*}} step %{{.*}} {
// CHECK:          %[[EXT:.*]] = xtile.extract %[[ARG0]][] [] [] : memref<i32> -> tensor<i32>
// CHECK:          %[[BCAST:.*]] = stablehlo.broadcast_in_dim %[[EXT]], dims = [] : (tensor<i32>) -> tensor<4x4xi32>
// CHECK:          xtile.insert %[[BCAST]] into %[[ARG1]][%{{.*}}, %{{.*}}] [4, 4] [1, 1] : tensor<4x4xi32> -> memref<128x64xi32>

// NEW-VECTOR:    func.func @wrapped_fusion_impl(
// NEW-VECTOR-SAME: %[[ARG0:[^:]+]]: memref<i32>,
// NEW-VECTOR-SAME: %[[ARG1:[^:]+]]: memref<128x64xi32>,
// NEW-VECTOR-SAME: %{{.*}}: index)
// NEW-VECTOR:      scf.for %[[IDX:.*]] = %{{.*}} to %{{.*}} step %{{.*}} {
// NEW-VECTOR:        %[[READ:.*]] = vector.transfer_read %[[ARG0]][], %{{.*}} : memref<i32>, vector<i32>
// NEW-VECTOR:        %[[BCAST:.*]] = vector.broadcast %[[READ]] : vector<i32> to vector<4x4xi32>
// NEW-VECTOR-NOT:    scf.if
// NEW-VECTOR:        vector.transfer_write %[[BCAST]], %[[ARG1]][%{{.*}}, %{{.*}}] {in_bounds = [true, true]} : vector<4x4xi32>, memref<128x64xi32>

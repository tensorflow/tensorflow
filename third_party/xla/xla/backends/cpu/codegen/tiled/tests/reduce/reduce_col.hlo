// RUN: fusion_to_xtile %s | fusion_compiler_opt \
// RUN:   --xtile-cpu-xtile-to-vector --xla-simplify-arith \
// RUN:   | FileCheck %s --check-prefix=CHECK-VECTOR
// RUN: fusion_to_xtile %s | fusion_compiler_opt \
// RUN:   --xtile-cpu-vector-to-llvm \
// RUN:   | FileCheck %s --check-prefix=CHECK-LLVM
// RUN: test_correctness %s --abs_error_bound=1e-5

add {
  x = f32[] parameter(0)
  y = f32[] parameter(1)
  ROOT add = f32[] add(x, y)
}

fusion {
  p0 = f32[128,256] parameter(0)
  c0 = f32[] constant(0)
  ROOT reduce = f32[256] reduce(p0, c0), dimensions={0}, to_apply=add,
   backend_config={sizes:[16]}
}

// CHECK-VECTOR: func.func @wrapped_fusion_impl

// CHECK-VECTOR-DAG: %[[C0:.*]] = arith.constant 0 : index
// CHECK-VECTOR-DAG: %[[C1:.*]] = arith.constant 1 : index
// CHECK-VECTOR-DAG: %[[C8:.*]] = arith.constant 8 : index
// CHECK-VECTOR-DAG: %[[CST:.*]] = arith.constant dense<0.000000e+00> : vector<8xf32>
// CHECK-VECTOR-DAG: %[[CST_0:.*]] = arith.constant dense<0.000000e+00> : vector<16x8xf32>

// CHECK-VECTOR: scf.for %[[IV0:.*]] = %[[C0]] to %{{.*}} step %[[C1]] {
// CHECK-VECTOR:   %[[LOOP_RES:.*]] = scf.for %[[IV1:.*]] = %[[C0]] to %[[C8]] step %[[C1]] iter_args(%[[ACC:.*]] = %[[CST_0]]) -> (vector<16x8xf32>) {
// CHECK-VECTOR:     %[[READ:.*]] = vector.transfer_read %arg0[%{{.*}}, %{{.*}}], %{{.*}} {in_bounds = [true, true]} : memref<128x256xf32>, vector<16x8xf32>
// CHECK-VECTOR:     %[[ACC_NEXT:.*]] = arith.addf %[[ACC]], %[[READ]] : vector<16x8xf32>
// CHECK-VECTOR:     scf.yield %[[ACC_NEXT]] : vector<16x8xf32>
// CHECK-VECTOR:   vector.extract %[[LOOP_RES]][0] : vector<8xf32> from vector<16x8xf32>

ENTRY main {
  p0 = f32[128,256] parameter(0)
  ROOT fusion = f32[256] fusion(p0), kind=kLoop, calls=fusion,
    backend_config={
      "fusion_backend_config":{
        "block_level_fusion_config":{
          "output_tiles":[{"sizes":["8"]}],
          "num_ctas":"1"
        }
      }
    }
}

// CHECK-LLVM-LABEL: llvm.func internal @wrapped_fusion_impl
// CHECK-LLVM-NOT: llvm.alloc

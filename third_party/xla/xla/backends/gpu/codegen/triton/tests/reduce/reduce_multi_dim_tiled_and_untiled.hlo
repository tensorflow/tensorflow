// RUN: fusion_to_triton --xla_gpu_experimental_enable_tiling_propagation %s 2>&1 | FileCheck %s
// RUN: not triton_test_correctness --xla_gpu_experimental_enable_tiling_propagation --abs_error_bound=1e-4 --rel_error_bound=1e-4 %s

// CHECK: MapSymbolIdToSequentialDimValue failed for symbolic dimension

region {
  p0 = f32[] parameter(0)
  p1 = f32[] parameter(1)
  ROOT add = f32[] add(p0, p1)
}

fusion {
  p = f32[8,16,12,32,16] parameter(0)
  c = f32[] constant(0)
  ROOT reduce = f32[16,32] reduce(p, c), dimensions={0,2,4}, to_apply=region,
    backend_config={sizes:[8,4,8]}
}

ENTRY main {
  p = f32[8,16,12,32,16] parameter(0)
  ROOT fusion = f32[16,32] fusion(p), kind=kCustom, calls=fusion,
    backend_config={
      "fusion_backend_config":{
        "kind":"__triton",
        "block_level_fusion_config":{
          "output_tiles":[{"sizes":["8", "16"]}],
          "num_warps":"1",
          "num_ctas":"1",
          "num_stages":"1"}}}
}

diff --git a/shardy/dialect/mpmd/transforms/export/lower_to_fragment_calls.cc b/shardy/dialect/mpmd/transforms/export/lower_to_fragment_calls.cc
index 4c7a09c3..18891fc2 100644
--- a/shardy/dialect/mpmd/transforms/export/lower_to_fragment_calls.cc
+++ b/shardy/dialect/mpmd/transforms/export/lower_to_fragment_calls.cc
@@ -418,7 +418,8 @@ class LowerToFragmentCallsPass
             OpBuilder meshBuilder(&ctx);
             meshBuilder.setInsertionPointToStart(module_op.getBody());
             sdy::MeshOp::create(meshBuilder, module_op.getLoc(),
-                                fragmentMeshName, fragmentMesh);
+                                fragmentMeshName, fragmentMesh,
+                                /*sym_visibility=*/nullptr);
           }
         };
 
diff --git a/shardy/dialect/mpmd/transforms/import/generate_sdy_meshes_from_topology_pass.cc b/shardy/dialect/mpmd/transforms/import/generate_sdy_meshes_from_topology_pass.cc
index 75b3f91b..89df8964 100644
--- a/shardy/dialect/mpmd/transforms/import/generate_sdy_meshes_from_topology_pass.cc
+++ b/shardy/dialect/mpmd/transforms/import/generate_sdy_meshes_from_topology_pass.cc
@@ -53,7 +53,8 @@ sdy::MeshOp GetOrCreateEmptyMesh(ModuleOp module_op) {
   OpBuilder builder = OpBuilder::atBlockBegin(module_op.getBody());
   sdy::MeshOp empty_mesh =
       sdy::MeshOp::create(builder, module_op.getLoc(), sdy::kEmptyMeshSymbol,
-                          sdy::MeshAttr::get(builder.getContext(), {}));
+                          sdy::MeshAttr::get(builder.getContext(), {}),
+                          /*sym_visibility=*/nullptr);
 
   // TODO(b/441487083): Look up the mesh in the global mesh registry.
   // Insert empty mesh into the topology attribute.
@@ -93,7 +94,8 @@ class GenerateSdyMeshesFromTopologyPass
     builder.setInsertionPointToStart(module_op.getBody());
     for (NamedMeshAttr named_mesh_attr : topology.getMeshes()) {
       sdy::MeshOp::create(builder, module_op.getLoc(),
-                          named_mesh_attr.getName(), named_mesh_attr.getMesh());
+                          named_mesh_attr.getName(), named_mesh_attr.getMesh(),
+                          /*sym_visibility=*/nullptr);
     }
 
     sdy::transformShardings(module_op, [&](sdy::TensorShardingAttr sharding) {
diff --git a/shardy/dialect/sdy/ir/ops.td b/shardy/dialect/sdy/ir/ops.td
index d3581f1c..2d4d6a1e 100644
--- a/shardy/dialect/sdy/ir/ops.td
+++ b/shardy/dialect/sdy/ir/ops.td
@@ -31,7 +31,9 @@ include "shardy/dialect/sdy/ir/op_interface.td"
 class Sdy_Op<string mnemonic, list<Trait> traits = []> :
    Op<Sdy_Dialect, mnemonic, traits>;
 
-def Sdy_MeshOp : Sdy_Op<"mesh", [Symbol, HasParent<"ModuleOp">]> {
+def Sdy_MeshOp : Sdy_Op<"mesh", [
+    SymbolName, SymbolVisibility, Symbol, HasParent<"ModuleOp">
+]> {
   let summary = "Named mesh";
   let description = [{
     Defines a new named mesh. All meshes in a module must have the same number
@@ -42,9 +44,11 @@ def Sdy_MeshOp : Sdy_Op<"mesh", [Symbol, HasParent<"ModuleOp">]> {
 
   let arguments = (ins
     SymbolNameAttr:$sym_name,
-    Sdy_Mesh:$mesh);
+    Sdy_Mesh:$mesh,
+    OptionalAttr<StrAttr>:$sym_visibility
+  );
 
-  let assemblyFormat = "$sym_name `=` $mesh attr-dict";
+  let assemblyFormat = "($sym_visibility^)? $sym_name `=` $mesh attr-dict";
   let hasVerifier = 1;
 }
 
diff --git a/shardy/dialect/sdy/transforms/export/resolve_permutation_factors.cc b/shardy/dialect/sdy/transforms/export/resolve_permutation_factors.cc
index 804550a2..fac82091 100644
--- a/shardy/dialect/sdy/transforms/export/resolve_permutation_factors.cc
+++ b/shardy/dialect/sdy/transforms/export/resolve_permutation_factors.cc
@@ -528,7 +528,8 @@ MeshOp getOrCreateMesh(Location loc, ModuleOp module, MeshAttr meshAttr,
   auto emplaceResult = meshCache.try_emplace(meshAttr, nullptr);
   if (emplaceResult.second) {
     OpBuilder moduleBuilder(module.getBodyRegion());
-    MeshOp meshOp = MeshOp::create(moduleBuilder, loc, baseName, meshAttr);
+    MeshOp meshOp = MeshOp::create(moduleBuilder, loc, baseName, meshAttr,
+                                   /*sym_visibility=*/nullptr);
     // Insert the op and rename if needed to avoid name collisions.
     symbolTable.insert(meshOp, module.getBody()->begin());
     emplaceResult.first->second = meshOp;
diff --git a/shardy/dialect/sdy/transforms/export/utils.cc b/shardy/dialect/sdy/transforms/export/utils.cc
index 8e719ebe..c37c86d6 100644
--- a/shardy/dialect/sdy/transforms/export/utils.cc
+++ b/shardy/dialect/sdy/transforms/export/utils.cc
@@ -409,7 +409,8 @@ FlatSymbolRefAttr getOrCreateMeshSymbol(Location loc, ModuleOp module,
     }
   }
   OpBuilder moduleBuilder(module.getBodyRegion());
-  MeshOp newMeshOp = MeshOp::create(moduleBuilder, loc, "mesh", meshAttr);
+  MeshOp newMeshOp = MeshOp::create(moduleBuilder, loc, "mesh", meshAttr,
+                                    /*sym_visibility=*/nullptr);
   symbolTable.insert(newMeshOp, module.getBody()->begin());
   return FlatSymbolRefAttr::get(module.getContext(), newMeshOp.getSymName());
 }
diff --git a/shardy/dialect/sdy/transforms/export/utils_test.cc b/shardy/dialect/sdy/transforms/export/utils_test.cc
index 9d08f825..2da7508a 100644
--- a/shardy/dialect/sdy/transforms/export/utils_test.cc
+++ b/shardy/dialect/sdy/transforms/export/utils_test.cc
@@ -288,7 +288,8 @@ TEST_F(ExportUtilsTest, GetOrCreateMeshSymbolGlobalMesh) {
 
   OpBuilder builder(moduleOp.get().getBodyRegion());
   MeshOp globalMeshOp = MeshOp::create(builder, moduleOp.get().getLoc(),
-                                       "global_mesh", globalMeshAttr);
+                                       "global_mesh", globalMeshAttr,
+                                       /*sym_visibility=*/nullptr);
   symbolTable.insert(globalMeshOp);
 
   // Calling getOrCreateMeshSymbol with the matching inlined MeshAttr should
@@ -322,7 +323,8 @@ TEST_F(ExportUtilsTest, GetOrCreateMeshSymbolExistingMaximalMesh) {
 
   OpBuilder builder(moduleOp.get().getBodyRegion());
   MeshOp maximalMeshOp = MeshOp::create(builder, moduleOp.get().getLoc(),
-                                        "maximal_mesh", maximalMeshAttr);
+                                        "maximal_mesh", maximalMeshAttr,
+                                        /*sym_visibility=*/nullptr);
   symbolTable.insert(maximalMeshOp);
 
   FlatSymbolRefAttr sym =
diff --git a/shardy/dialect/sdy/transforms/import/lift_inlined_meshes.cc b/shardy/dialect/sdy/transforms/import/lift_inlined_meshes.cc
index 96aece14..f1219fba 100644
--- a/shardy/dialect/sdy/transforms/import/lift_inlined_meshes.cc
+++ b/shardy/dialect/sdy/transforms/import/lift_inlined_meshes.cc
@@ -45,7 +45,8 @@ namespace {
 
 MeshOp createNewMeshOp(Location loc, MeshAttr mesh, OpBuilder& builder) {
   auto createMesh = [&](StringRef meshName) {
-    return MeshOp::create(builder, loc, meshName, mesh);
+    return MeshOp::create(builder, loc, meshName, mesh,
+                          /*sym_visibility=*/nullptr);
   };
   if (std::optional<int64_t> deviceId = mesh.getMaximalDeviceId()) {
     std::string meshName = llvm::formatv("maximal_mesh_{0}", deviceId);
@@ -60,7 +61,8 @@ MeshOp createNewMeshOp(Location loc, MeshAttr mesh, OpBuilder& builder) {
 MeshOp createNewMeshOp(Location loc, mlir::stablehlo::MeshAttr mesh,
                        OpBuilder& builder) {
   auto createMesh = [&](StringRef meshName, MeshAttr sdyMesh) {
-    return MeshOp::create(builder, loc, meshName, sdyMesh);
+    return MeshOp::create(builder, loc, meshName, sdyMesh,
+                          /*sym_visibility=*/nullptr);
   };
   SmallVector<MeshAxisAttr> sdyAxes;
   for (auto axisAttr : mesh.getAxes()) {
diff --git a/third_party/llvm/generated.patch b/third_party/llvm/generated.patch
index 858d6908..69b62be2 100644
--- a/third_party/llvm/generated.patch
+++ b/third_party/llvm/generated.patch
@@ -224,6 +224,32 @@ diff -ruN --strip-trailing-cr a/clang/unittests/Analysis/LifetimeSafetyTest.cpp
      Analysis =
          std::make_unique<LifetimeSafetyAnalysis>(*AnalysisCtx, nullptr, LSOpts);
      Analysis->run();
+diff -ruN --strip-trailing-cr a/libcxx/include/tuple b/libcxx/include/tuple
+--- a/libcxx/include/tuple
++++ b/libcxx/include/tuple
+@@ -1396,7 +1396,7 @@
+                   "Attempted construction of reference element binds to a temporary whose lifetime has ended");
+   }
+ #      endif // _LIBCPP_STD_VER >= 23
+-  return std::apply([]<class... _Args>(_Args&&... __args) { return _Tp(std::forward<_Args>(__args)...); },
++  return std::apply([]<class... _Args>(_Args&&... __args) -> _Tp { return _Tp(std::forward<_Args>(__args)...); },
+                     std::forward<_Tuple>(__tuple));
+ }
+ 
+diff -ruN --strip-trailing-cr a/libcxx/test/std/utilities/tuple/tuple.tuple/tuple.apply/make_from_tuple.pass.cpp b/libcxx/test/std/utilities/tuple/tuple.tuple/tuple.apply/make_from_tuple.pass.cpp
+--- a/libcxx/test/std/utilities/tuple/tuple.tuple/tuple.apply/make_from_tuple.pass.cpp
++++ b/libcxx/test/std/utilities/tuple/tuple.tuple/tuple.apply/make_from_tuple.pass.cpp
+@@ -255,5 +255,10 @@
+     test_perfect_forwarding();
+     test_noexcept();
+ 
++  { // Test that constructing a T& returns the same object
++    int i;
++    assert(&std::make_from_tuple<const int&>(std::tuple<int&>(i)) == &i);
++  }
++
+   return 0;
+ }
 diff -ruN --strip-trailing-cr a/llvm/lib/Target/X86/X86InstrInfo.cpp b/llvm/lib/Target/X86/X86InstrInfo.cpp
 --- a/llvm/lib/Target/X86/X86InstrInfo.cpp
 +++ b/llvm/lib/Target/X86/X86InstrInfo.cpp
@@ -294,7 +320,7 @@ diff -ruN --strip-trailing-cr a/llvm/lib/Transforms/Vectorize/SLPVectorizer.cpp
    /// Values used only by @llvm.assume calls.
    SmallPtrSet<const Value *, 32> EphValues;
  
-@@ -25883,7 +25891,17 @@
+@@ -25891,7 +25899,17 @@
          assert((!isa<ExtractElementInst>(Scalar) ||
                  !IgnoredExtracts.contains(cast<ExtractElementInst>(Scalar))) &&
                 "Extractelements should not be replaced.");
diff --git a/third_party/llvm/workspace.bzl b/third_party/llvm/workspace.bzl
index 5a905de8..5056deba 100644
--- a/third_party/llvm/workspace.bzl
+++ b/third_party/llvm/workspace.bzl
@@ -19,8 +19,8 @@ load("//third_party:repo.bzl", "tf_http_archive")
 
 def repo(name):
     """Imports LLVM."""
-    LLVM_COMMIT = "43574226b712f94de17f07392a64282ac8ddab23"
-    LLVM_SHA256 = "9e261fbbebd60809cf985075904f59a46b89de9ea114b5938ba9a6c8e9202e75"
+    LLVM_COMMIT = "9ebb067a8a2b4b0705f06d59c77d36dfab98333f"
+    LLVM_SHA256 = "84f6624d9f6a32e180d7cb3b91d9e47b3f51da7f1f79ef216614dbff8b928145"
 
     tf_http_archive(
         name = name,
diff --git a/third_party/stablehlo/temporary.patch b/third_party/stablehlo/temporary.patch
index fff6bb6d..b00c1c36 100644
--- a/third_party/stablehlo/temporary.patch
+++ b/third_party/stablehlo/temporary.patch
@@ -550,7 +550,12 @@ diff --ruN a/stablehlo/stablehlo/tests/interpret/reduce_scatter.mlir b/stablehlo
 diff --ruN a/stablehlo/stablehlo/tests/transforms/mesh_axes_replica_group_compatibility.mlir b/stablehlo/stablehlo/tests/transforms/mesh_axes_replica_group_compatibility.mlir
 --- stablehlo/stablehlo/tests/transforms/mesh_axes_replica_group_compatibility.mlir
 +++ stablehlo/stablehlo/tests/transforms/mesh_axes_replica_group_compatibility.mlir
-@@ -5,7 +5,7 @@
+@@ -1,11 +1,11 @@
+-// RUN: stablehlo-opt %s -stablehlo-compatibility-expander="target=1.14.0" -allow-unregistered-dialect | FileCheck %s
++// RUN: stablehlo-opt %s -stablehlo-compatibility-expander="target=1.14.0" | FileCheck %s
+ 
+ module {
+ 
  
    // CHECK-LABEL: @all_reduce_rgv3
    func.func @all_reduce_rgv3(%arg0: tensor<4xf32>) -> tensor<4xf32> {
@@ -577,7 +582,12 @@ diff --ruN a/stablehlo/stablehlo/tests/transforms/mesh_axes_replica_group_compat
      %0 = "stablehlo.all_to_all"(%arg0) {
        concat_dimension = 0 : i64,
        split_dimension = 0 : i64,
-@@ -44,7 +44,7 @@
+@@ -40,11 +40,11 @@
+ 
+     return %0 : tensor<4xf32>
+   }
+-  "sdy.mesh"() {sym_name = "sdy_mesh", stablehlo.mesh = {axes = [{name = "x", size = 2 : i64}, {name = "y", size = 2 : i64}]}} : () -> ()
++  func.func private @sdy_mesh() attributes {stablehlo.mesh = {axes = [{name = "x", size = 2 : i64}, {name = "y", size = 2 : i64}]}}
  
    // CHECK-LABEL: @all_reduce_sdy_mesh
    func.func @all_reduce_sdy_mesh(%arg0: tensor<4xf32>) -> tensor<4xf32> {
@@ -586,7 +596,12 @@ diff --ruN a/stablehlo/stablehlo/tests/transforms/mesh_axes_replica_group_compat
      %0 = "stablehlo.all_reduce"(%arg0) ({
      ^bb0(%arg1: tensor<f32>, %arg2: tensor<f32>):
        %1 = "stablehlo.add"(%arg1, %arg2) : (tensor<f32>, tensor<f32>) -> tensor<f32>
-@@ -59,7 +59,7 @@
+@@ -55,11 +55,11 @@
+     return %0 : tensor<4xf32>
+   }
+ 
+-  "sdy.mesh"() {sym_name = "sdy_mesh_dev", stablehlo.mesh = {axes = [{name = "x", size = 2 : i64}, {name = "y", size = 2 : i64}], device_ids = dense<[0, 2, 1, 3]> : tensor<4xi64>}} : () -> ()
++  func.func private @sdy_mesh_dev() attributes {stablehlo.mesh = {axes = [{name = "x", size = 2 : i64}, {name = "y", size = 2 : i64}], device_ids = dense<[0, 2, 1, 3]> : tensor<4xi64>}}
  
    // CHECK-LABEL: @all_reduce_sdy_mesh_dev
    func.func @all_reduce_sdy_mesh_dev(%arg0: tensor<4xf32>) -> tensor<4xf32> {
@@ -595,6 +610,15 @@ diff --ruN a/stablehlo/stablehlo/tests/transforms/mesh_axes_replica_group_compat
      %0 = "stablehlo.all_reduce"(%arg0) ({
      ^bb0(%arg1: tensor<f32>, %arg2: tensor<f32>):
        %1 = "stablehlo.add"(%arg1, %arg2) : (tensor<f32>, tensor<f32>) -> tensor<f32>
+@@ -70,7 +70,7 @@
+     return %0 : tensor<4xf32>
+   }
+ 
+-  "sdy.mesh"() {sym_name = "sdy_mesh_max", stablehlo.mesh = {axes = [], device_ids = dense<0> : tensor<1xi64>}} : () -> ()
++  func.func private @sdy_mesh_max() attributes {stablehlo.mesh = {axes = [], device_ids = dense<0> : tensor<1xi64>}}
+ 
+   // CHECK-LABEL: @all_reduce_sdy_mesh_max
+   func.func @all_reduce_sdy_mesh_max(%arg0: tensor<4xf32>) -> tensor<4xf32> {
 @@ -84,4 +84,56 @@
      } : (tensor<4xf32>) -> tensor<4xf32>
      return %0 : tensor<4xf32>

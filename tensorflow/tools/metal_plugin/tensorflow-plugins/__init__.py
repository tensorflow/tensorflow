# TensorFlow scans site-packages/tensorflow-plugins at import and loads every
# shared object it finds there. The directory has to be a package for the
# wheel to carry it, which is why this file exists; it is deliberately empty.

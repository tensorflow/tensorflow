op {
  graph_op_name: "StringUpper"
  summary: "Uppercases strings in `input`"
  description: <<END
Converts each lowercase character of each string in the input Tensor to
uppercase.

>>> strings = ['Hello', 'TensorFlow']
>>> tf.strings.upper(strings).numpy()
array([b'HELLO', b'TENSORFLOW'], dtype=object)

END
}

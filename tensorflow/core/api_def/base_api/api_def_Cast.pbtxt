op {
  graph_op_name: "Cast"
  summary: "Cast x of type SrcT to y of DstT."
  description: <<END
This operation takes an input tensor x and the data type of destination dtype,
and casts x to the dtype.

For Example:

>> x = tf.constant([1.8, 3.2], dtype=tf.float32)
>> tf.dtypes.cast(x, tf.int32) #[1,3], dtype=int32
END
}
